@model GradesVM

<div class="card box-shadow border-1 mt-4">
	<div class="card-header bg-secondary bg-gradient modal-lg-0 py-3">
		<div class="row">
			<div class="col-12 text-center">
				<h2 class="text-white py-2">@(Model.Grades.Id != 0 ? "Cập nhật" : "Thêm") điểm</h2>
			</div>
		</div>
	</div>
	<div class="card-body p-4">
		<form method="post" class="row">
			<div class="border p-3">
				<input asp-for="Grades.Id" type="hidden" />

				<div class="form-floating py-2 col-12">
					<select asp-for="Grades.StudentId" asp-items="@Model.StudentsList" class="form-select border-0 shadow" required>
						<option disabled selected>--Chọn sinh viên--</option>
					</select>
					<label asp-for="Grades.StudentId" class="ms-2"></label>
					<span asp-validation-for="Grades.StudentId" class="text-danger p-0"></span>
				</div>

				

				<div class="form-floating py-2 col-12">
					<select asp-for="Grades.SubjectId" asp-items="@Model.SubjectList" class="form-select border-0 shadow" required>
						<option disabled selected>--Chọn môn học--</option>
					</select>
					<label asp-for="Grades.SubjectId" class="ms-2"></label>
					<span asp-validation-for="Grades.SubjectId" class="text-danger p-0"></span>
				</div>

				<div class="form-floating py-2 col-12">
					<input asp-for="Grades.grade" class="form-control border-0 shadow" required>
					<label asp-for="Grades.grade" class="ms-2"></label>
					<span asp-validation-for="Grades.grade" class="text-danger p-0"></span>
				</div>

				<div class="row pt-2">
					<div class="col-6 col-md-3">
						<button type="submit" class="btn btn-secondary form-control">
							@(Model.Grades.Id != 0 ? "Cập nhật" : "Thêm")
						</button>
					</div>
					<div class="col-6 col-md-3">
						<a class="btn btn-outline-secondary form-control" asp-action="Index" asp-controller="Grades">
							Quay lại
						</a>
					</div>
				</div>
			</div>
		</form>
	</div>
</div>
@section Scripts {
	@{
		<partial name="_ValidationScriptsPartial" />
	}
}